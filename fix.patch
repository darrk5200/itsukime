--- a/index.js
+++ b/index.js
@@ -1230,7 +1230,20 @@ async function fetchChannelMessagesViaREST(channelId, token) {
                             break;
                         }
 
                         if (!res.ok) break;
+
+                        let batch;
+                        try {
+                            batch = await res.json();
+                        } catch (parseErr) {
+                            // Handle premature close or invalid JSON
+                            networkAttempt++;
+                            if (networkAttempt > MAX_NETWORK_RETRIES) throw parseErr;
+                            const backoff = Math.min(1000 * 2 ** (networkAttempt - 1), 30000);
+                            await new Promise(r => setTimeout(r, backoff));
+                            continue;
+                        }
 
-                        const batch = await res.json();
                         if (!Array.isArray(batch) || batch.length === 0) break;

